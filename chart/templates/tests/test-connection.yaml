apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "b2mml-batchml.fullname" . }}-test-connection"
  labels:
    {{- include "b2mml-batchml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "b2mml-batchml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
