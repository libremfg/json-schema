apiVersion: v1
kind: Service
metadata:
  name: {{ include "b2mml-batchml.fullname" . }}
  labels:
    {{- include "b2mml-batchml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "b2mml-batchml.selectorLabels" . | nindent 4 }}
